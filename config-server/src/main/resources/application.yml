spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Rishav30194/config-server
          clone-on-start: true

server:
  port: 8086

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka
